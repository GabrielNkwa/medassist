{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"npm --prefix artifacts/api-server run dev\" \"npm --prefix artifacts/medassist run dev\"",
    "dev:api": "npm --prefix artifacts/api-server run dev",
    "dev:frontend": "npm --prefix artifacts/medassist run dev",
    "build": "npm run build:frontend && npm run copy-to-public",
    "build:api": "npm --prefix artifacts/api-server run build",
    "build:frontend": "npm --prefix artifacts/medassist run build",
    "copy-to-public": "node copy-to-public.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && (npm --prefix artifacts/api-server run typecheck || echo 'API typecheck errors, continuing...') && (npm --prefix artifacts/medassist run typecheck || echo 'Frontend typecheck errors, continuing...')"
  },
  "private": true,
  "engines": {
    "node": ">=20.x"
  },
  "workspaces": [
    "artifacts/*",
    "lib/*"
  ],
  "dependencies": {
    "@vercel/node": "^5.8.12"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0",
    "prettier": "^3.8.3",
    "typescript": "~5.6.3"
  }
}
